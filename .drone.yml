---
kind: pipeline
type: docker
name: default

clone:
  disable: true

steps:
  - name: clone
    image: docker.io/bdebyl/alpine-git
    commands:
      - git clone https://github.com/debyltech/go-snipcart-webhook.git .
      - git checkout $DRONE_COMMIT
      - echo -n "0.1.$DRONE_BUILD_NUMBER,latest" > .tags

  - name: build
    image: golang:1.20.3-nanoserver
    commands:
      - CGO_ENABLED=0 go build -tags netgo

trigger:
  branch:
  - master
  event:
  - push
---
kind: signature
hmac: 868c254b7206bfbc2f9efd815822bbf10d6deaeb1b5862ff9f890bfc725c2af5

...
