---
kind: pipeline
type: docker
name: default

clone:
  disable: true

steps:
  - name: clone
    image: docker.io/bdebyl/alpine-git
    commands:
      - git clone https://github.com/debyltech/go-snipcart-webhook.git .
      - git checkout $DRONE_COMMIT
      - echo -n "0.1.$DRONE_BUILD_NUMBER,latest" > .tags

  - name: build
    image: docker.io/golang:1.20.3-nanoserver
    commands:
      - CGO_ENABLED=0 go build -tags netgo

trigger:
  branch:
  - master
  event:
  - push
---
kind: signature
hmac: e2febb5bb6a638749e8054a808dc7ddf17b8707cf3017a281a7610c78ac0e05c

...
